import React, {FC} from "react";

interface AsdProps {
}

type Props = AsdProps;

const Asd: FC<Props> = ({}) => {
    return (
        <div>
            asdasdasd page!
        </div>
    );
};

export default Asd;